// Move values around
const MOV = 'MOV'

// Arithmetic
const ADD = 'ADD'
const SUB = 'SUB'
const MUL = 'MUL'
const DIV = 'DIV'
const MOD = 'MOD'

// Jump and jump markers
const JMP = 'JMP'
const SEC = 'SEC'

// End marker
const HLT = 'HLT'

export { MOV, ADD, SUB, MUL, DIV, MOD, JMP, SEC, HLT }
